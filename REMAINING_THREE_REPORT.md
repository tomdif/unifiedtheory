# The Remaining Three — Closing Report

**2026-07-30.** Outcomes of the last three items from the breakthrough survey.

## 1. The d-dimensional Quantitative Hauptvermutung — DONE [LEAN]

`KFCausalCSpecHauptvermutungDim.lean`, axiom-clean. The only
dimension-dependent link of the 4D chain was the counting dictionary; the one
new fact needed is that **fractional roots never amplify relative error**
(`rpow_error_le`: |u^a - 1| <= |u - 1| for 0 < a <= 1). With it:

- `count_estimator_error_dim`: in dimension d, a Poisson-window count
  estimates any power tau^k (k <= d) with relative error eps via
  (n/(rho C_d))^(k/d).
- `quantitative_hauptvermutung_dim`: three counts -> metric component with
  error <= (3/2) eps S, for EVERY d >= 2; polarization (3/2) and
  Chebyshev-Poisson are dimension-free; only C_d changes.

Consequence: the resolution-limit structure (AM-GM, dimension-free) and the
clock-limit identity extend to all d — the causal-set closure exponents
l_p^((d-2)/(d-1)) T^(1/(d-1)) match the Salecker-Wigner clock bound in every
dimension, now with the reconstruction side formalized.

## 2. sin^2(theta_W) from the S3 sheet sector — CLOSED NEGATIVELY [LEAN]

`KFCausalSheetGaugeNoGo.lean`, axiom-clean. The committed order sector puts
the full-S3 monodromy on the 2-dimensional zero-sum carrier (the standard
irrep), generated by the loop holonomies (01) and (12). Any continuous gauge
group acting on that sector must commute with the transport. Computed:

- `S1_sq`, `S2_sq`, `S1_mul_S2_cube`: the holonomies are involutions with an
  order-3 composite — the standard S3 irrep, verified.
- **`sheet_commutant_trivial`** (Schur, by direct linear algebra): any real
  matrix commuting with both holonomies is a scalar.

**Verdict: the sheet sector admits at most a one-parameter continuous
symmetry. No SU(2) doublet structure can act there; no sin^2(theta_W) can be
derived from the S3 monodromy sector as committed.** The alpha(0) = ln(5/3)/70
falsification gate ("derive the adjoint reps or the RG claim is falsified")
is NOT rescued from this sector — the hope is closed formally, not left open.
Any rescue must come from elsewhere (e.g. genuinely new continuum structure),
and the honest default remains: the RG unification claim stays falsified.

## 3. The Hermite-Biehler instability no-go — FORMULATED, OPEN [PHYS]

The target: prove that Lorentz invariance + discreteness + convergence to box
FORCES the ASS-observed instability (complex zeros of the retarded symbol
with future-timelike imaginary part), via a Hermite-Biehler-type argument:
retarded stability requires the symbol to be zero-free in the upper half
plane (an HB/Pick property), while the physically forced Mellin zeros at
s = 1/2, 1, 3/2 constrain the symbol's asymptotic phase along the real axis
in a way that appears incompatible with zero-freeness.

Status after scoping: this is genuinely a complex-analysis campaign
(argument principle on the Laplace/Mellin strip for these specific
transforms), not a session-scale composition. What IS in hand:

- The real-mode symbol h(s) on exponential test modes e^(st) is a cone
  integral our machinery can evaluate; its s -> 0 behavior (-s^2, from the
  operator theorem) and UV limit (-4/sqrt6 * sqrt(rho) < 0) have the SAME
  sign, so there is no forced REAL growing mode: any instability is
  necessarily oscillatory-growing (complex) — consistent with and sharpening
  ASS's numerics. Formalizing h(s) < 0 for all real s >= 0 is the natural
  first rung and reuses the operator chain.
- The precise no-go conjecture: no kernel with M[K](1/2) = M[K](1) =
  M[K](3/2) = 0, M[K](2) = -1 (the forced structure) has a retarded symbol
  zero-free in the closed upper half-plane.

This is the one item of the three left open, stated as a conjecture with a
concrete first rung, rather than claimed.

## Ledger after this session

| item | status |
|---|---|
| d-dimensional QH | derived, axiom-clean |
| sin^2(theta_W) from S3 | **no-go theorem** — gate stays falsified |
| Hermite-Biehler no-go | formulated + first rung identified; open |
