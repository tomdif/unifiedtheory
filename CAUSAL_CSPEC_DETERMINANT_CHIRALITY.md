# CSpec determinant-line chirality

## Result

The finite global CSpec atlas already recovers an intrinsic permutation of
three continuation-distinguishable directions on every chart edge. This
module proves that the orientation variable needed to protect chirality is
not additional microscopic data. It is the sign representation of that same
permutation:

```text
Xi(edge) = sign(sheetTransport(edge)).
```

The two-dimensional zero-sum sheet carrier carries the standard
representation of `S3`. Its unique parity-odd operator is

```text
J = [[1,-2],[2,-1]].
```

For each recovered edge transport `U_e`, Lean proves

```text
(sign(U_e) J) U_e = U_e J.
```

Thus the sign line and the internal carrier are not separately postulated
objects. They are two representations of one continuation-derived CSpec
permutation.

## Sequential law

Starting from `Xi=1`, a chart history generates its orientation recursively:

```text
Xi(h,e) = sign(U_e) Xi(h).
```

The formalization proves at every finite depth:

```text
Xi(h)^2 = 1,
Xi(h) != 0,
O_(n+1) V_n = V_n O_n,
```

where `O_n=Xi iJ` is the relational chirality observable on each history
fiber. On the first continuation-derived unfilled CSpec loop, the computed
orientation is exactly

```text
Xi(loop) = -1.
```

This is the precise finite answer to the earlier orientation-source problem:
the nonzero relative chirality sign is generated by CSpec sheet holonomy.
Global complex conjugation still exchanges the two overall conventions, so
the theorem fixes the arrow/chirality correlation rather than an absolute
human label called left or right.

## Genuine CPTP record channel

For `N` finite histories, flatten the direct-sum carrier as

```text
Fin N x Fin 2 ~= Fin (N*2).
```

Let `P_h` project onto the two-dimensional block belonging to history `h`.
Lean proves

```text
sum_h P_h^dagger P_h = I.
```

Therefore

```text
Phi(rho) = sum_h P_h rho P_h
```

is CPTP in the repository's Choi/Kraus definition. Its action is exact:

```text
Phi(rho)[(h,a),(k,b)] = rho[(h,a),(k,b)]  if h=k,
                       0                   otherwise.
```

It erases coherence between different history records while preserving the
complete internal two-dimensional block. In particular,

```text
Phi(Xi iJ) = Xi iJ.
```

This is a direct-sum record construction, not an assumed global
system-environment tensor factor. It therefore avoids the composability wall
identified for an independently postulated protected tensor product.

## What is newly closed

The following finite chain is now theorem-level:

```text
native CSpec continuation data
  -> three-sheet permutation U_e
  -> determinant orientation sign(U_e)
  -> nonzero history orientation Xi
  -> exact relational chirality transport
  -> state-independent CPTP history recording that preserves chirality.
```

The earlier abstract bundle theorem assumed the orientation flip and had only
an entrywise/idempotent pinching. This module derives the flip from the CSpec
edge and supplies a complete Kraus representation.

## Honest boundary

The result is finite and applies to the explicit global CSpec atlas already
constructed in the repository. It does not prove:

- that every physical unlabeled causal-set growth trajectory generates this
  atlas or its full `S3` monodromy;
- that trace preservation of the density-matrix channel is the same as
  preserving quantum-measure normalization `D(Omega,Omega)` on the event
  algebra;
- a continuum limit, Standard-Model weak-current map, or experimental
  chirality prediction.

The next genuine physics theorem must derive the atlas/channel as the
universal observable quotient of physical causal births, not merely exhibit
one finite realization.

## Verification

The implementation is
`UnifiedTheory/Audit/KFCausalCSpecDeterminantChirality.lean`. The headline
theorems are checked with no `sorry` and no custom axioms; the printed axiom
footprints contain only the repository's standard foundational axioms.
