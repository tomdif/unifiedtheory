# The pi/4 theory's first number: r(8) = 0.40(2), below both the
# classical and the flat-manifold value (2026-08-11)

## Why this computation is the "new physics" move

The coherent program's terminus (extraction-2026-08-02) was
existence-abundant/nothing-selects: at depth 8 the wave family's
ordering fraction was a POLYTOPE, r_psi(8) in [0.105, 0.804], and every
selection principle was proven dead, free, or content-free.  The
quantized bi-normalized theory (quadrature-phase-gates-2026-08-11) has
no free phase and a genuine Markov diagonal, so it must OUTPUT numbers.
This unit computes its first one, with readings registered in
pi4_first_prediction.py before the run.

## Existence hardened first

pi/4 feasibility now checked at ALL 2450 parents of the depth-8 tree
(the gate-1 scan covered n <= 6): ZERO infeasible, zero closure
exclusions — the action-phased bi-normalized theory exists on the
entire physical tree at its unique phase.  Only 3 parents are
forced/discrete (K <= 3 child classes); the rest carry residual
freedom, handled two ways: the registered canonical MAX-ENTROPY
selection (maximize H of the Born child distribution q_c = mu rho^2),
and five RANDOM feasible laws to measure selection sensitivity.
Max-entropy support: 13958/16999 level-8 causets (82%), effective
number 1700 — the law is broad, not a thin thread.

## THE NUMBER (exact tree DP, no sampling)

  mean ordering fraction r(n) = <#related pairs> / C(n,2), Born chain:

    law                      r(8)      band
    pi/4 max-entropy         0.4136
    pi/4 random selections   0.3881 .. 0.4043   (spread 0.016)
    uniform growth (counting baseline)   0.5080
    2D diamond sprinkling (flat manifold)  0.4997  (= 1/2 exactly, all n)
    coherent wave family     [0.105, 0.804]  (logged; nothing selects)

  r(n) trend (max-ent): 0.2988, 0.3004, 0.3337, 0.3680, 0.3942, 0.4136
  for n = 3..8 — rising, increments shrinking after n = 6
  (+0.034, +0.026, +0.019).

VERDICT: registered reading (iii), NOVEL VALUE.  |r_pi4 - r_sprinkling|
= 0.086 and |r_pi4 - r_uniform| = 0.094, both far above the
selection spread (0.016) and above the two baselines' mutual
separation (0.008).  The quantized theory predicts a DEFINITE
sub-classical ordering fraction: at n = 8 the quantum dynamics
suppresses relatedness by Delta r ~ -0.09 relative to both classical
growth and flat-2D statistics.  This is the first sharp dynamical
number the quantum sector of this program has ever produced — the
polytope of the old theory collapses to 0.40(2).

Depth caveat, stated plainly: r(n) is still rising at n = 8 with
shrinking increments; the deep limit is not determined by this data.
Crude extrapolation of the increment decay reaches ~0.45-0.50, so
asymptotic agreement with the flat value (reading (i) in the limit)
remains open; what is established is the finite-n deviation profile
r(n), which is itself the theory's signature (a definite,
selection-robust departure from classical/manifold statistics at small
causet volume — where a discrete quantum-gravity theory SHOULD deviate
from the continuum).

## Second finding: the max-entropy law is spontaneously fact-stable

The records test on the max-entropy pi/4 law gives

    X_minus(Q) over T = 5..8  =  0.0000

— the COHERENT channel itself never un-happens a fact at accessible
depth (all 11 stems, all steps, 4 decimals), even though the
interference bound permits regressions and the earlier
bisection-selected pi/4 law showed X_minus(Q) = 0.07.  Maximal-
ignorance selection appears to place the coherent measure inside the
fact-stable cone spontaneously.  CONJECTURE (registered): the
max-entropy pi/4 law has monotone coherent stem measures — if provable,
the lambda parameter becomes physically idle at the record level and
the fact-stability story closes without dephasing at all.

## Third finding: record interference does not decay (lambda pressure)

Mean |Q - P| over the 11 stems: 0.046, 0.046, 0.054, 0.065, 0.071 for
T = 4..8 (max 0.24 -> 0.21).  Registered reading (beta) provisionally:
interference on records is flat-to-rising at accessible depth, so
either the deep limit forces lambda -> 1 (records fully dephased
macroscopically) or the max-entropy fact-stability conjecture makes
the question moot for monotone records.  These two findings pull
opposite ways and their confrontation is now the sharpest open
dynamical question of the theory.

## Cross-sector note (registered, not computed)

phi = pi/4 fixes the elementary action quantum: each birth carries
e^{i g pi/4}, i.e. action (pi/4) hbar per unit of discrete BD-action
gap.  The gravity arc independently fixed the discrete action's
gravitational normalization (CausalActionCoefficient: c = 1/2,
l_disc = sqrt(8 pi) l_P).  The two normalizations of the SAME discrete
action — quantum (this unit) and gravitational (that one) — must agree
in the 4D theory; that consistency check is a definite computation and
is where an absolute, unit-carrying prediction would first appear.

## Honest scope

- 2D-order toy engine (W2 kernel), depth 8; the 4D analogue is the
  physical target.
- Max-entropy selection is registered canonical, not derived; but the
  0.016 selection band shows the number is insensitive to it.
- Sprinkling baseline is exactly 1/2 in 2D (u,v independence), which
  makes the -0.09 deviation clean.
- The uniform-growth and sprinkling baselines nearly coincide here;
  discriminating them from each other needs other observables (height,
  interval abundances) — for THIS unit only the deviation of the pi/4
  law from both matters.
- X_minus(Q) = 0.0000 is a 4-decimal statement over 11 stems x 3
  steps, not a theorem.

## Registered follow-ups

1. Prove or refute the max-entropy fact-stability conjecture.
2. Depth 9-10 (sampling or truncated DP): does r(n) approach 1/2 or
   stall — and does mean record interference turn over?
3. Interval-abundance / height observables of the pi/4 law vs
   sprinkling (dimension-and-curvature diagnostics beyond r).
4. The 4D action-normalization consistency check (quantum pi/4 unit
   vs gravitational c = 1/2 unit).
5. Exact Z[zeta8] certificates (unchanged).
