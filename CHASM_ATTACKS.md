# The three chasm attacks (2026-08-16): one narrowing negative,
# one symmetry hierarchy, one partial closure

## Chasm 1 - the dimensional bridge: NEGATIVE, but eliminative

Member x rate scan (maxent/xfe/minH x n/5, n/7; n=48, MEMBER env
verified working, popcount fixed): the IR collapse of the interval-
dimension profile is MEMBER-INDEPENDENT and RATE-INDEPENDENT -
every combination decays to d ~ 1.75-1.91 at k~32 while the UV
reaches up to 3.3 (n/5).  The registered hypothesis "the IR
collapse belongs to the maxent selection" is REFUTED: the collapse
is structural to free-downset growth + double conservation.
Consequence: the 2D->4D bridge is NOT hiding in the selection band
or in constant expansion rates.  Remaining candidates: scale-
hierarchical width rules, different weight families, or genuinely
larger scales.  The chasm stands, two of its suspects eliminated.

## Chasm 2 - matter/gauge: a symmetry hierarchy, measured

Born-weighted clock moments m_k of the stationary state (n=12..19,
150 paths): the moment algebra is FULLY DETERMINED BY PARITY
(m_{k+4} = -m_k, m_3 = -conj(m_1), Re m_2 = 0 - all forced, all
verified; m_4 = -1.0000 exactly = the parity theorem) except TWO
free parameters:
  - coherence residue m_1 = +0.1353(28)  [numerically eerie:
    e^{-2} = 0.13534 - FLAGGED as curiosity, not claimed];
  - quadrature imbalance Im m_2 = -0.0118(21): a ~5 sigma BREAKING
    of conjugation symmetry at the 1% level.
Pattern: Z8 clock symmetry -> Z2 exact (parity), conjugation
weakly broken - a SYMMETRY HIERARCHY, the first emergent-symmetry
structure of the theory and the natural seed for a gauge sector.

## Chasm 3 - the selection band: PARTIAL CLOSURE by causality

Bell-violation score per member (15 parents, n=13; corrected patch
after catching a silent no-op replace that had voided two runs -
members verified genuinely divergent, TV up to 0.79):
  maxent 0.423(39)   xfe 0.452(60)   minH 3.05(50)
CAUSALITY EXCLUDES THE CONCENTRATED END OF THE BAND (7x worse) and
places maxent at the causal optimum, statistically tied with xfe.
"Least Bell violation" is a principled selection criterion that
closes most of the band; separating maxent from xfe needs either
more statistics or the kappa-type observables where they diverge.
