# The decoherence functional of covariant growth: anti-decoherence (2026-08-03)

Candidate A of the unknown-physics probe - the last untested corner
of the program's two-question core.  decoherence_functional.log.

## Structure theorem (four lines, from covariance)

All labeled paths to the same class carry the identical amplitude
A(C)e^{i(S-1)phi} (the coboundary property), so with covariant
final-class identification the decoherence functional between
stage-n events at horizon T telescopes to

  D_T(A,B) = sum_{C_T} |A(C_T)|^2 N_A(C_T) N_B(C_T),

with N_A the labeled path counts through A.  Every term is
nonnegative: EVENT-LEVEL INTERFERENCE IN THIS THEORY IS ALWAYS
CONSTRUCTIVE, the quantum measure is everywhere superadditive, and
decoherence can only mean weighted future-orthogonality of sectors.

## Measurement (stage-4 action sectors, horizons T = 4..7,
   phi = 0.9, max-min member, labeled counting)

Normalized off-diagonals d(A,B):
  mean:  0 (T=4, trivial) -> 0.0835 -> 0.1209 -> 0.1532
  strongest pair d(S=-2,S=2): 0.277 -> 0.528 -> 0.604.
MONOTONE GROWTH in every robustly-populated pair.

## Verdict (pre-registered reading (ii), amplified)

Pure covariant growth contains NO internal decoherence mechanism at
accessible depth - and the trend is ANTI-decoherence: action sectors
re-cohere as the universe grows.  Combined with the structure
theorem, the theory as it stands moves AWAY from classical
additivity with volume.  Classicality therefore requires an
ingredient the theory does not contain - OR a different event
identification / history counting: N is a labeled count, so this
functional sits on the history-counting axiom (the FOURTH
independent trail to that door: selection, quadrature, Lambda,
decoherence), and the orbit-counted D is the registered next
measurement of this thread.

## Scope

Depth <= 7, one member, one phase, one stage partition, labeled
counting.  The directional claim (monotone growth over three
horizon steps) is the finding; extrapolation is not.

## The program's two-question core, updated

1. What counts as a history?  (Now reached by four independent
   trails.)
2. What makes histories decohere?  Answer at accessible depth:
   nothing in the theory - the first genuinely discovery-shaped
   negative of the arc: the missing ingredient is real, and naming
   it is the program's forward problem.
