# Completion attempt #1: fact-stability selection (P4) - proposed and unsupported (2026-08-03)

## The proposal

The program's two crises are shaped like each other's solution: the
selection crisis (the covariant polytope is vast; every physical
selection principle proved impossible, free, or content-free) and
the fact crisis (no internal mechanism makes facts: anti-decoherence
in both countings; record measures churn).  The most economical
completion available:

  P1 (kinematics): time is a partial order; covariant single-element
      growth.
  P2 (dynamics): the wave family - rho e^{i dS/hbar}, Markov sum
      rule, covariance - with its proven phase diagram (windows /
      resonances / spines), obligations (aging), and prohibitions
      (Bell, cluster, composable branching; preclusion structure =
      exactly the no-go certificates).
  P3 (counting): the history-identity axiom, both versions defined,
      invariants classified by the positivity principle.
  P4 (NEW): reality is a FACT-STABILIZING member: stem-event
      measures converge along growth; the classical world is the
      stabilized record structure.  One postulate answering both
      crises - IF such members exist.

## The pre-registered test and its verdict

Member-dependence of stem-measure churn X (completion_p4_test.log):
deep member and 12 random vertices cluster in X = 1.17..1.46
(spread 1.25x); the maxmin member is an outlier at 3.04 - in the
WRONG direction.  No low-churn member found; the generic churn level
(~1.2 over 11 stems x 2 horizon steps) is far from any stability
floor.  Reading (iii) leaning (ii): churn is only weakly
member-dependent among sampled members, and P4's existence
requirement is UNSUPPORTED at accessible depth and sampling.

RECORD CORRECTION carried by this scan: the records test
(records-churn-2026-08-03) used the churniest member sampled
(maxmin, 2.5x typical); its qualitative non-convergence verdict
stands (typical churn is still large), but its magnitudes overstate
the polytope norm.

Residual avenue, registered honestly: true churn MINIMIZATION over
the polytope (nonconvex; vertices sampled here were not optimized) -
the 13/14 clustering makes a dramatic hidden minimum unlikely but
not excluded.  Depth escape (stabilization beyond n = 7) likewise
open, likewise a computation.

## Standing of the theory after the attempt

The theory now resists completion by everything assembled from its
own parts: internal dynamics (anti-decoherence), covariant records
(churn), and selection-by-stability (this test).  What survives as
the completed core: P1-P3 with the proven phase diagram, three no-go
theorems, the necessity theorem, the m-adic and parity laws, the
positivity principle - and two primitives the theory cannot supply
for itself: the identity of histories, and whatever makes facts.
Completion attempt #1 is closed negative; the honest statement is
that the missing ingredient is not a selection among the solutions,
and the next attempt must modify the sum rule itself - a different
theory, constrained by everything this one proved.
