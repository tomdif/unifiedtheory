# Causal record dynamics

## Result

The sharp native effect used by the causal resolution law is no longer an
independent probability postulate.  It follows from a recorded microscopic
dynamics with two properties:

1. refinement is lossless, so its recorded Stinespring map `V` is an
   isometry;
2. the identity of the causal child is transported through refinement:

   `Q_c V = V P_c`.

Here `P_c` restricts the incoming native-child carrier to child `c`, while
`Q_c` reads the same child in the newly created record.  Lean proves

```text
V† V = I  and  Q_c V = V P_c
  ==> K_c† K_c = P_c.
```

If the coherent record counit also recovers the unresolved parent,

```text
E V = I,
```

then the operators themselves are forced:

```text
K_c = P_c = |c><c|.
```

Thus the native resolution law is derived from lossless refinement,
coherent recovery, and conservation of causal-record identity.  No separate
Born-effect, matrix-support, locality, or nondemolition axiom enters the
microscopic theorem.

The quantum outcome is also explicit. For every density matrix `rho`,

```text
Pr(c | rho) = Re Tr(rho K_c† K_c) = Re rho[c,c].
```

In particular, a system prepared in native child `d` yields `c=d` with
probability one and every `c≠d` with probability zero. This outcome theorem is
derived from isometry and causal-record transport through the sharp effect;
it is not separately assumed.

## Further deductions

The derived state has four exact consequences.

- **Repeatability:** `K_c K_c = K_c`.
- **Native superselection:** the reduced channel is exactly dephasing in the
  physical-child basis.
- **Protected algebra:** a state or observable is fixed exactly when all its
  cross-child entries vanish.  The protected finite algebra is therefore the
  classical algebra generated by the native child projectors.
- **Complementarity boundary:** the two Pauli-`Y` orientation characters are
  identified by native-record resolution if placed in the same carrier.
  Chirality must occupy a separate protected algebra, be diagonalized in a
  larger record, or be lost.

## Independence theorem

The alignment equation is indispensable. The repository contains an exact
two-outcome counterexample formed by the positive and negative orientation
projectors. Its recorded refinement satisfies

```text
V† V = I,
E V = I,
binary relabeling covariance,
```

but fails `Q_c V = V P_c`; equivalently, `K_c† K_c` is not the projector
onto native child `c`. Consequently, losslessness, exact coherent parent
recovery, and label covariance do not determine which sharp basis nature
records. This is a constructive independence proof, not merely the absence
of a derivation.

## Honest physical boundary

The canonical history representation now closes the alignment bridge for
every already-realized finite cylinder.  In
`KFCausalCylinderRecordTransport.lean`, sequential growth itself defines

```text
V_n |h> = sum_b a(h,b)|h,b>,
```

and prefix retention proves `Q_c V_n = V_n P_c` for the projector onto any
cylinder `c` and the projector onto all of its continuations.  Local Born
normalization makes `V_n` isometric and therefore derives the exact pullback
effect `V_n† Q_c V_n = P_c`.  Every physical child is transported this way
from the first rank after its birth onward.

The chirality obstruction is now resolved at the finite carrier level, but
not yet at the laboratory level.  `KFCausalBundleProtectedChirality.lean`
replaces same-carrier dephasing by a direct-sum carrier whose blocks are
history records and whose internal fibers are the intrinsic rank-two sheet
carrier.  Prefix retention again derives `Q_c V = V P_c`, now with the
internal fiber carried along.  Block pinching in the history label erases
cross-history entries but fixes every within-history operator.

Full `S_3` holonomy makes the ordinary commutant scalar, so a nontrivial
globally invariant chirality operator is impossible.  The formal escape is
unique up to scale: in the standard sheet carrier the parity-odd generator

```text
J = [[1,-2],[2,-1]]
```

anticommutes with both adjacent transpositions.  Relative to the intrinsic
Gram form, `iJ` is Hermitian and `(iJ)^2=3I`.  If the causal orientation label
`Xi` reverses on an odd sheet edge, then the two signs cancel and the
relational observable `Xi iJ` is transported exactly through sequential
growth at every rank.  History-record formation can therefore preserve
chirality because chirality is internal to each record block, not an
off-diagonal entry between different records.

The remaining input is narrower and explicit.  Growth does not assign a
future-child projector before that child is born, and it does not by itself
prove that a laboratory apparatus couples to the intrinsic cylinder algebra.
The bundle theorem also requires actual causal/CSpec edges to realize the
already formalized odd sheet transports and their orientation reversal.  Its
generic block pinching is proved idempotent and trace-preserving, but is not
yet promoted in this module to a dynamically derived state-independent CPTP
instrument.  Thus sequential growth derives nondemolition transport of
realized causal facts and the exact operator consequence of sign-twisted
holonomy, while physical edge realization, laboratory embedding, and
continuum weak-current interpretation remain separate bridge theorems.

The machine-checked implementation is
`UnifiedTheory/Audit/KFCausalNativeRecordDynamics.lean`.

The sequential-growth derivation is documented in
`CAUSAL_CYLINDER_RECORD_TRANSPORT.md`.

The protected bundle extension is documented in
`CAUSAL_BUNDLE_PROTECTED_CHIRALITY.md`.
